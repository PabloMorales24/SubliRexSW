<!DOCTYPE html>
<html lang="es" data-bs-theme="light">
<head>
  <meta charset="UTF-8">
  <title>Gestión de Usuarios - SubliRex</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      display: flex;
    }

    .sidebar {
      width: 250px;
      min-height: 100vh;
      background-color: #2b2e62;
      color: white;
      position: fixed;
      padding-top: 1rem;
    }

    .sidebar a {
      color: white;
      text-decoration: none;
      display: block;
      padding: 10px 20px;
    }

    .sidebar a:hover {
      background-color: #00bb00;
    }

    .sidebar .logo {
      display: flex;
      justify-content: center;
      margin-bottom: 2rem;
    }

    .sidebar .logo img {
      width: 180px;
      object-fit: contain;
    }

    .main-content {
      margin-left: 250px;
      width: 100%;
    }

    .topbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      background-color: var(--bs-body-bg);
      border-bottom: 1px solid #ccc;
    }
  </style>
</head>
<body>

  <!-- Sidebar -->
  <div class="sidebar">
    <div class="logo">
      <a href="/dashboard">
        <img src="/img/logo.png" alt="SubliRex Logo">
      </a>
    </div>

    <a href="/dashboard">📊 Dashboard</a>
    <a href="/compras">🛒 Compras</a>
    <a href="/ventas">💰 Ventas</a>
    <a href="/pedidos">📦 Pedidos</a>
    <a href="/inventario">📋 Inventario</a>
    <a href="/informacion">ℹ️ Información</a>
    <% if (user.rol_id === 1) { %>
      <a href="/usuarios">👥 Usuarios</a>
    <% } %>
  </div>

  <!-- Main Content -->
  <div class="main-content">

    <!-- Topbar -->
    <div class="topbar">
      <div></div> <!-- espacio izquierdo -->
      <div class="d-flex align-items-center gap-3">
        <div>
          Bienvenido <strong><%= user.nombre %></strong>
        </div>
        <a href="/logout" class="btn btn-outline-primary btn-sm">Cerrar sesión</a>
        <div class="form-check form-switch ms-3">
          <input class="form-check-input" type="checkbox" id="themeToggle">
          <label class="form-check-label" for="themeToggle">🌙</label>
        </div>
      </div>
    </div>

    <!-- Page Content -->
    <div class="p-4">
      <h2>📋 Panel de Gestión de Usuarios</h2>
      <p class="text-muted">Aquí podrás ver, crear, editar y eliminar usuarios.</p>

      <div class="alert alert-success mt-4">
        ✅ Has sido redirigido correctamente a esta vista porque tu rol es <strong>Administrador</strong>.
      </div>
    </div>
  </div>

  <!-- Scripts -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    const toggle = document.getElementById('themeToggle');
    const root = document.documentElement;

    const current = localStorage.getItem('srex-theme');
    if (current) {
      root.setAttribute('data-bs-theme', current);
      toggle.checked = current === 'dark';
    }

    toggle.addEventListener('change', () => {
      const theme = toggle.checked ? 'dark' : 'light';
      root.setAttribute('data-bs-theme', theme);
      localStorage.setItem('srex-theme', theme);
    });
  </script>
</body>
</html>
