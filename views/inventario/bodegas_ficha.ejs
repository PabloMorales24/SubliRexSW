<%- include('../partials/header') %>

<div class="container py-4">
  <h2 class="h4 mb-3">Ficha de bodega</h2>

  <div class="card mb-4">
    <div class="card-body">
      <h5 class="card-title"><%= bodega.nombre %></h5>
      <p class="mb-1"><strong>Ubicación:</strong> <%= bodega.ubicacion || '—' %></p>
      <p class="mb-3"><strong>Descripción:</strong> <%= bodega.descripcion || '—' %></p>
      <hr>
      <p><strong>Creado por:</strong> <%= bodega.creado_por_nombre || 'N/D' %></p>
      <p><strong>Creado en:</strong> <%= bodega.creado_en.toLocaleString() %></p>
      <p><strong>Actualizado por:</strong> <%= bodega.actualizado_por_nombre || '—' %></p>
      <p><strong>Actualizado en:</strong> <%= bodega.actualizado_en.toLocaleString() %></p>
    </div>
  </div>

  <a href="/inventario/bodegas" class="btn btn-secondary">Volver</a>
  <a href="/inventario/bodegas/<%= bodega.id %>/editar" class="btn btn-primary ms-2">Editar</a>
</div>

<%- include('../partials/footer') %>
